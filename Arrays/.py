word1 = "stew"
word2 = "list"

space_string = " " *(len(word1) - len(word2))
word2 += space_string
string = ""

for i in range(len(word1)):
    string += word1[i]
    string += word2[i]
    

print(string)
    
if !char:
    print("crud")
